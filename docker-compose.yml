version: "3"
services:
  backend:
    build: .
    container_name: schennel_node
    environment:
      - 'NODE_ENV=production'
    depends_on:
      - db
    restart: 'on-failure:5'
    ports:
      - 3921:8000
    networks:
      - net
    
  db:
    image: mysql
    container_name: schennel_mysql
    restart: unless-stopped
    volumes:
      - schennel-mysql:/var/lib/mysql
    environment:
      TZ: Asia/Seoul
      MYSQL_ROOT_PASSWORD: Super-Easy-Passw0rd
    networks:
      - net

volumes:
  schennel-mysql:
networks:
  net: